import React from 'react';

describe('<CheckItem/>', () => {
    test.todo(
        'Removes check from, and disables, "checked"-checkbox when NA is checked'
    );
    test.todo('Hides metatable when NA is checked');
});
